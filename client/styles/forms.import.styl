label
  margin-bottom 10px

input
select
  font-weight normal

label
  > span
    display block
    margin-bottom 3px
  &.outside
    margin 0

.file-upload
  margin-bottom 10px

input[typ=submit]
  +above(1)
    font-size 14px

.radio
  &:last-of-type
    margin-bottom 0
