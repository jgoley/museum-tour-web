@import 'tour_edit.import'

.tap-edit-list
  margin-bottom 30px
  list-style-type none
  border-top 2px solid $primary
  border-bottom 2px solid $primary
  > li
    position relative
    border-bottom 1px solid lighten($secondary, 30%)
    padding 5px 3%
    +mobile()
      padding 10px 3%
    &:last-of-type
      border 0
  .stop-number
  .title
    font-weight bold
    cursor pointer
    font-size 1.5em
    margin-right 3px
    +below(2)
      font-size 1em
    &:hover
      color $primary

.tour-edit-actions
  centerVertically()
  right 3%

.edit-tour
  color $body-text
  &:hover
    color $primary
    text-decoration none
  .sub-title
    +mobile()
      display inline-block

.delete-tour
  font-size 1.5em
  color $m-gray
  &:hover
    color $primary

.tour-type-icon
  margin-left 5px
  font-size 1.4em
  +below(2)
    font-size 1em

.create-tour
  width 50%
  max-width 500px
  +between(2,3)
    width 75%
  +mobile()
    width 100%
  button
    margin-top 10px

